# Chapter 8: dependable local use

Active work order: KIRBY2-DAILY-008, revision 1. Implements the ordered A–D
sections and C8-01–C8-06 of the revision-3 product plan. The preserved starting
state is recorded in `kirby2-chapter8-build/baseline.json` in the authorized
artifact directory. Earlier uncommitted chapter work remains owned and intact.

Backend ownership: bounded portable training evidence, semantic verification,
recovery through existing `release.backup` / `release.restore`, and audits.
UI ownership: installed first use, optional capability adapter, Library utility
controls, global worker fencing, protected non-image interaction tests.

Ordered delivery:

1. Reuse the existing backup format for a bounded C2–C7 training export. Include
   all supported dependencies and editable notes in their separate areas. Mark
   exported reserved cases revealed before export; preserve that history on
   recovery. Refuse missing, incompatible, truncated or changed source evidence.
2. Provide inspection, export and recovery to a NEW data folder from Library.
   Never overwrite the active folder. No live attempt is resumed from a backup.
   Add an explicit installed-launch data-folder option and start ordinary launches
   at Home. Keep old backends usable with clear optional-capability refusal.
3. Exercise a clean installed-package workflow, interrupted publication/study,
   index reconstruction, bounded workload, and keyboard/focus/geometry behavior.
4. Publish source-bound evidence, a capability ledger and an unrun release plan.

Deviation D8-01: the user's explicit prohibition on ALL images supersedes the
planned renders. Image generation/capture/inspection and visible Qt are forbidden.
Use protected offscreen text/interaction/geometry checks only. Model visual and
human acceptance stay NOT_EXERCISED, without claiming they passed.

Deviation D8-02: the user prohibits worker contact. Work and source review are
local; no Sol/Terra dispatch or independent-model acceptance is claimed.

No push, commit, release/remote qualification, network services, broker, empirical
market claim, updater, portfolio expansion or work beyond Chapter 8 is authorized.
